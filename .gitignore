# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Regenerable static-plot review artifacts (bin/render_static_samples.py)
samples_static_modernize/

# Scratch figure output (tools/local-plot/figview.sh) — never published, never committed
tmp-figdir/

# Interrogation destination docs (/interrogate) — alignment artifacts, not code
.interrogate-*.md

# Excalidraw MCP server log — dropped in whatever cwd the canvas server ran from
excalidraw.log
